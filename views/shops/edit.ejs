<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Shop Item</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit Shop Item</h1>
    <form action="/admins/shops/<%= shopItem._id %>/edit" method="POST">
        <div>
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= shopItem.name %>" required>
        </div>
        <div>
            <label for="type">Type:</label>
            <input type="text" id="type" name="type" value="<%= shopItem.type %>" required>
        </div>
        <div>
            <label for="price">Price:</label>
            <input type="number" id="price" name="price" value="<%= shopItem.price %>" required>
        </div>
        <div>
            <label for="imageUrl">Image URL:</label>
            <input type="text" id="imageUrl" name="imageUrl" value="<%= shopItem.imageUrl %>" required>
        </div>
        <button type="submit">Save Changes</button>
    </form>
</body>
</html>