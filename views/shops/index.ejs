<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Shop's Items</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>

    <h1>Here's all the Shop's Items</h1>

    <ul>
        <% if (shops.length > 0) { %>
            <% shops.forEach((shop) => { %>
                <li>
                    <a href="/shops/<%= shop._id %>"><%= shop.name %> </a>
                </li>
            <% }) %>
        <% } else { %>
            <li> Out of Stock. 
        <% } %>
    </ul>
</body>

</html>